source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in authorio.gemspec.
gemspec

gem 'jbuilder', '~> 2.0'

group :development do
  gem 'sqlite3'
  gem 'sprockets'
  gem 'pry'
end
